server {
    listen 80;
    server_name localhost;

    location / {
        add_header Content-Type application/json;
        return 200 '{"status":"Fortress-monastery authentication active"}';
    }

    location /health {
        add_header Content-Type application/json;
        return 200 '{"status":"ok"}';
    }
}
