server {
    listen 80;
    server_name localhost;

    location / {
        add_header Content-Type application/json;
        return 200 '{"status":"Feeding on your biomass...","fleet":"kraken"}';
    }

    location /health {
        add_header Content-Type application/json;
        return 200 '{"status":"ok"}';
    }
}
